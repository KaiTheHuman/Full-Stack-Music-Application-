import React from "react";

const Home = () => { //Homepage, only displays a welcome message
    return (
        <div>
            <h1>Welcome to Homepage</h1>
        </div>
    );
};

export default Home;
